module.exports = {
    content: [
      './src/**/*.{html,js}', // Specifies the files where Tailwind should look for classes
    ],
    theme: {
      extend: {
        'transform': 'transform',
      },
    },
    plugins: [],
  };